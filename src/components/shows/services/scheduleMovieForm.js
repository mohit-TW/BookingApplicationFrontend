
export const initialValues = {
  movieId: null,
  slotId: null,
  scheduleDate: null,
  cost: null,
};

// export const formSchema = object({
//   cost: number().required("Cost is required").test(/^[0-9\b]+$/),
//   //password: string("Enter password").required("Password is required"),
// });
